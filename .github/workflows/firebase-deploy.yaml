name: Deploy to Firebase

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18'

      - name: Install Dependencies
        run: npm install

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Build the app
        run: npm run build

      # Set Firebase service account credentials
      - name: Set Firebase Service Account Credentials
        run: |
          echo "${{ secrets.FIREBASE_SERVICE_ACCOUNT }}" > $GITHUB_WORKSPACE/firebase-service-account.json

      # Set GOOGLE_APPLICATION_CREDENTIALS environment variable
      - name: Set GOOGLE_APPLICATION_CREDENTIALS
        run: |
          export GOOGLE_APPLICATION_CREDENTIALS="$GITHUB_WORKSPACE/firebase-service-account.json"

      # Deploy to Firebase Hosting and Functions using service account credentials
      - name: Deploy to Firebase Hosting and Functions
        run: |
          firebase deploy --only hosting,function --project dev --force
